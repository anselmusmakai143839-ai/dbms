#include <stdio.h>

int main() {
    FILE *fp;
    int size;

    fp = fopen("sample.txt", "r");

    fseek(fp, 0, SEEK_END);
    size = ftell(fp);

    printf("File size: %d bytes", size);

    fclose(fp);
    return 0;
}


nput (sample.txt)
Hello

Output
File size: 5 bytes